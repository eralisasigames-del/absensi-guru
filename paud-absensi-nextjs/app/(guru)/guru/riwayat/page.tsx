import { requireUser } from "@/lib/auth"; import LogoutButton from "@/components/logout-button";
export default async function Riwayat(){const {supabase,profile}=await requireUser();const {data}=await supabase.from("attendance").select("*").eq("user_id",profile.id).order("tanggal",{ascending:false}).limit(100);return <main className="p-5 md:p-10"><div className="max-w-5xl mx-auto"><div className="flex justify-between mb-6"><h1 className="text-2xl font-black">Riwayat Absensi</h1><LogoutButton/></div><div className="overflow-x-auto rounded-2xl border bg-white"><table className="w-full text-sm"><thead><tr className="border-b text-left"><th className="p-4">Tanggal</th><th>Hadir</th><th>Pulang</th></tr></thead><tbody>{(data||[]).map((x:any)=><tr className="border-b" key={x.id}><td className="p-4">{x.tanggal}</td><td>{x.jam_hadir?new Date(x.jam_hadir).toLocaleTimeString("id-ID"):"-"}</td><td>{x.jam_pulang?new Date(x.jam_pulang).toLocaleTimeString("id-ID"):"-"}</td></tr>)}</tbody></table></div></div></main>}
