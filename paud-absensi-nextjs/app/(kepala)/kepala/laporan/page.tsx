import { requireUser } from "@/lib/auth"; import ExportButton from "./export-button";
export default async function Laporan(){const {supabase,profile}=await requireUser();if(profile.role!=="kepala_sekolah")return <main className="p-10">Akses ditolak.</main>;const {data}=await supabase.from("attendance").select("tanggal,jam_hadir,jam_pulang,profiles(nama)").order("tanggal",{ascending:false}).limit(500);return <main className="p-5 md:p-10"><div className="max-w-6xl mx-auto"><div className="flex justify-between mb-6"><h1 className="text-3xl font-black">Laporan Absensi</h1><ExportButton rows={data||[]}/></div><div className="overflow-x-auto rounded-2xl border bg-white"><table className="w-full text-sm"><thead><tr className="border-b text-left"><th className="p-4">Guru</th><th>Tanggal</th><th>Hadir</th><th>Pulang</th></tr></thead><tbody>{(data||[]).map((x:any,i:number)=><tr className="border-b" key={i}><td className="p-4">{x.profiles?.nama}</td><td>{x.tanggal}</td><td>{x.jam_hadir?new Date(x.jam_hadir).toLocaleTimeString("id-ID"):"-"}</td><td>{x.jam_pulang?new Date(x.jam_pulang).toLocaleTimeString("id-ID"):"-"}</td></tr>)}</tbody></table></div></div></main>}
