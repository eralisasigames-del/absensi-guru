import { requireUser } from "@/lib/auth"; import LogoutButton from "@/components/logout-button";
export default async function Kepala(){const {supabase,profile}=await requireUser();if(profile.role!=="kepala_sekolah")return <main className="p-10">Akses ditolak.</main>;const today=new Date().toLocaleDateString("en-CA",{timeZone:"Asia/Jakarta"});const [{data:teachers},{data:attendance},{count}]=await Promise.all([supabase.from("profiles").select("*").eq("role","guru").eq("status","aktif"),supabase.from("attendance").select("*").eq("tanggal",today),supabase.from("leave_requests").select("*",{count:"exact",head:true}).eq("status","pending")]);return <main className="min-h-screen p-5 md:p-10"><div className="max-w-6xl mx-auto"><header className="flex justify-between mb-8"><div><p className="text-sm font-semibold text-emerald-700">DASHBOARD KEPALA SEKOLAH</p><h1 className="text-3xl font-black">Monitoring Hari Ini</h1></div><LogoutButton/></header><div className="grid grid-cols-3 gap-4 mb-6"><div className="bg-white border rounded-2xl p-5"><p className="text-gray-500">Total Guru</p><b className="text-3xl">{teachers?.length||0}</b></div><div className="bg-white border rounded-2xl p-5"><p className="text-gray-500">Hadir</p><b className="text-3xl">{attendance?.filter((x:any)=>x.jam_hadir).length||0}</b></div><div className="bg-white border rounded-2xl p-5"><p className="text-gray-500">Menunggu Approval</p><b className="text-3xl">{count||0}</b></div></div><div className="grid md:grid-cols-2 gap-4"><a href="/kepala/pengajuan" className="rounded-2xl bg-white border p-6 font-bold">📝 Pengajuan Izin/Cuti</a><a href="/kepala/laporan" className="rounded-2xl bg-white border p-6 font-bold">📊 Laporan Absensi</a></div></div></main>}
